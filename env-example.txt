# EcoTrade - Ejemplo de variables de entorno
# Copia este archivo a `.env` y completa los valores.
# IMPORTANTE: no subas `.env` con secretos reales.

# ===== Base de datos (Prisma + PostgreSQL) =====
# Ejemplo (local): postgresql://USER:PASSWORD@localhost:5432/ecotrade?schema=public
# Ejemplo (Supabase): postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/ecotrade?schema=public

# ===== Servidor =====
PORT=5000
NODE_ENV=development

# ===== Seguridad / Auth =====
JWT_SECRET=change_me_in_production
JWT_EXPIRATION=24h

# ===== Demo mode =====
# true = usa controladores y store demo (para tests y sandbox)
DEMO_MODE=false

# ===== Rate limit tracking público de reciclaje =====
# Ventana y máximo para GET /api/recycling/submissions/code/:code
RECYCLING_PUBLIC_TRACKING_RL_WINDOW_MS=900000
RECYCLING_PUBLIC_TRACKING_RL_MAX=30

# ===== Recibo verificable (Solana) =====
# Si está deshabilitado, la verificación no intentará emitir receipt.
SOLANA_RECEIPTS_ENABLED=false

# devnet o mainnet-beta
SOLANA_CLUSTER=devnet

# Opcional. Si no se define, se usa clusterApiUrl(cluster)
# SOLANA_RPC_URL=https://api.devnet.solana.com

# Payer secret en UNO de estos formatos:
# - JSON array: [1,2,3,...]
# - base64: (bytes de secretKey)
# SOLANA_PAYER_SECRET=

# Tip: genera un payer para dev con:
#   npm run solana:keygen

# ===== Bootstrap super admin (solo inicialización) =====
SUPER_ADMIN_EMAIL=admin@ecotrade.com
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=change_me
SUPER_ADMIN_COUNTRY=AR
