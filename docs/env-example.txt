# Configuraciones de base de datos
DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/ecotrade?schema=public
PORT=5000

# Configuraciones de Firebase
FIREBASE_SERVICE_ACCOUNT={"type": "service_account", ...}

# Configuraciones de seguridad
JWT_SECRET=tu_secret_muy_secreto_y_largo
JWT_EXPIRATION=7d

# CORS (obligatorio en producción). Lista separada por comas.
# Ejemplo: https://ecotrade.com,https://www.ecotrade.com
CORS_ORIGINS=http://localhost:3000

# Modo demo (NO usar en producción)
DEMO_MODE=false

# Configuraciones de entorno
NODE_ENV=development

# Supabase (Auth + Storage)
# URL del proyecto (Settings → API)
SUPABASE_URL=

# Service role key (Settings → API) - SOLO backend (nunca en frontend)
SUPABASE_SERVICE_ROLE_KEY=

# Bucket para imágenes (Storage → Buckets)
SUPABASE_STORAGE_BUCKET=ecotrade.media

# Bootstrap del primer super_admin (solo para inicialización)
# Uso: npm run bootstrap:superadmin
SUPER_ADMIN_EMAIL=admin@ecotrade.com
SUPER_ADMIN_USERNAME=superadmin
# Recomendado >= 16 caracteres (evitar patrones comunes)
# Sugerencia: no lo guardes en archivo; configúralo como secreto del entorno
SUPER_ADMIN_PASSWORD=
# Opcional (por defecto AR)
SUPER_ADMIN_COUNTRY=AR
